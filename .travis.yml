language: node_js
node_js:
  - '10'
notifications:
  email: false

cache:
  yarn: true
  directories:
    - library/common/node_modules

before_install:
  - cd library/common

install:
  - yarn install

script:
  - yarn lint
  - yarn build
  - yarn test
